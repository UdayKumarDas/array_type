require "spec_helper"

describe ArrayType do
  it "has a version number" do
    expect(ArrayType::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
